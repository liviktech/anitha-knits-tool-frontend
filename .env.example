# Must include the /api/v1 prefix — every fetch call in src/ appends its path
# directly to this value, e.g. VITE_API_BASE_URL + '/production/extruder'.
VITE_API_BASE_URL="https://your-backend-service.onrender.com/api/v1"
